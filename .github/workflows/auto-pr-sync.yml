name: 🔄 TEST PARSING SUCCESS

on:
  push:
    branches:
      - 'Updates/**' 
      
jobs:
  test-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Success Check
        run: echo "Workflow started successfully."
